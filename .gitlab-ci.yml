image: node:latest

cache:
  paths:
    - node_modules/

stages:
  - build
  - deploy

before-script:
    - sudo apt install yarn

build:
  stage: build
  
  script:
    - echo "building from src"
    - yarn -v
    - yarn install
    - yarn run build
  artifacts:
    path: 
      - ./dist