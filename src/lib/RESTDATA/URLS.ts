const PROTOCOL = "http";

const BASE_URL = "api.class.ce.kntu.ac.ir";

export const URLS = {
  POSTUSER: `${PROTOCOL}://${BASE_URL}/signup`,
  LOGINUSER: `${PROTOCOL}://${BASE_URL}/login`,
  RETRIVEUSER: `${PROTOCOL}://${BASE_URL}/auth`,
  GETCLASSROOMS: `${PROTOCOL}://${BASE_URL}/classrooms`,
};
